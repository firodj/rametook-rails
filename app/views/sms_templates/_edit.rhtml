<td valign="top" align="center">
<div id="sms_template_offset_<%= @sms_template.id %>"></div>
</td>
<td style="padding:10px;" colspan="2">
<% form_for :sms_template, :url => {:action => 'edit', :id => @sms_template.id}, :html => { :multipart => false } do |f| -%>
  <%= error_messages_for 'sms_template' %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <input type="hidden" id="page" name="page" value="<%= params[:page] %>" />
  <%= submit_tag print_words('save').capitalize %>
<% end -%>
</td>
<td valign="top" align="center">
<%= print_words('cancel').capitalize_words %> <%= link_to_remote image_tag('icon_close.png'), :url => {:action => 'close', :id => @sms_template.id} %> 
</td>
