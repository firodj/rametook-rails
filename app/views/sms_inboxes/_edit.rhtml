<td valign="top" align="center">
<div id="sms_inbox_offset_<%= @sms_inbox.id %>"></div>
</td>
<td style="padding:10px;" colspan="6">
<% form_for :sms_inbox, :url => {:action => 'edit', :id => @sms_inbox.id}, :html => { :multipart => false } do |f| -%>
  <%= error_messages_for 'sms_inbox' %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <input type="hidden" id="page" name="page" value="<%= params[:page] %>" />
  <%= submit_tag print_words('save').capitalize %>
<% end -%>
</td>
<td valign="top" align="center">
<%= print_words('cancel').capitalize_words %> <%= link_to_remote image_tag('icon_close.png'), :url => {:action => 'close', :id => @sms_inbox.id} %> 
</td>
